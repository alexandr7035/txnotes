<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alexandr7035.txnotes">

    <application
        android:name="com.alexandr7035.txnotes.TXNotesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.alexandr7035.txnotes.EditNoteActivity"></activity>
        <activity android:name="com.alexandr7035.txnotes.ShowNoteActivity" />
        <activity android:name="com.alexandr7035.txnotes.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.alexandr7035.txnotes.CreateNewNoteActivity"
            android:parentActivityName="com.alexandr7035.txnotes.MainActivity" />
    </application>

	<uses-permission android:name="android.permission.VIBRATE"/>
</manifest>
